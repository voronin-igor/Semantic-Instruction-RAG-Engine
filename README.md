# Semantic-Instruction-RAG-Engine
A local prototype of a RAG system based on the LlamaIndex framework and the ChromaDB vector database. The project implements semantic search based on text instructions without external API dependencies demonstrates document indexing with metadata extraction of relevant contexts, and custom post‑processing of responses without AI hallucinations
